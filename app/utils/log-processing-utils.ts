export enum Actions {
    BET = "bets",
    CALL = "calls",
    FOLD = "folds",
    RAISE = "raises",
    POST = "posts",
    CHECK = "checks",
}

export enum Streets {
    PREFLOP = "Preflop",
    FLOP = "Flop:",
    TURN = "Turn:",
    RIVER = "River:",
}

